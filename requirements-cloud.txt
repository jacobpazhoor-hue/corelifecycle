# Python deps the GitHub Actions render runner installs (free cloud render).
# The cloud REGENERATES audio (edge-tts + numpy) and RENDERS — it never touches YouTube,
# so no Google/OAuth libs are needed here (upload happens locally after review).
numpy
scipy
soundfile
edge-tts
imageio-ffmpeg
# gate.py's crayon style checks (flat fill / camera lock) read rendered stills via PIL.
# Missing this HALTed the cloud build on 2026-08-14 and forced a local render.
pillow
